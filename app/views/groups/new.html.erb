<% @image_filenames.each do |filename| %>
    <img src="<%= asset_path(filename) %>" alt="<%= filename %>" width="5%">
<% end %>

<%= render "form", group: @group %>

<br>

<div>
  <%= link_to "Back to cathegories", groups_path %>
</div>
